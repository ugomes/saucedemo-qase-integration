{
  "name": "saucedemo-qase-cypress",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "cy:open": "cypress open",
    "cy:run:auth": "cypress run --spec cypress/e2e/auth/auth.cy.js",
    "cy:run:inventory": "cypress run --spec cypress/e2e/inventory/inventory.cy.js",
    "cy:run:all": "cypress run",
    "qase:upload": "node scripts/qase-upload.js",
    "test:auth:qase": "npm run cy:run:auth && npm run qase:upload",
    "test:inventory:qase": "npm run cy:run:inventory && npm run qase:upload",
    "test:all:qase": "npm run cy:run:all && npm run qase:upload"
  },

  "private": true,
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "cypress": "^15.8.2",
    "mocha-junit-reporter": "^2.2.1"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "dotenv": "^17.2.3",
    "fast-xml-parser": "^5.3.3"
  }
}
